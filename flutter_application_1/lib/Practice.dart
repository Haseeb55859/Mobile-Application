import 'package:flutter/material.dart';

class NamePrint extends StatelessWidget {
  const NamePrint({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       appBar: AppBar(
        title: 
       ),
    );
  }
}
